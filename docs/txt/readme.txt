TopSecret documentation starter file

Use the command: ./gradlew run
to run the program.

Use the command: ./gradlew run --args="--change-password"
to change your password.

Use the command: ./gradlew clean run --args="--web"
to run the web version.

Username must be all lowercase.
Password must be at least 5 characters with at least one uppercase and one lowercase letter.

Current usernames: jonah, kevin, patrick
Current passwords: Password

